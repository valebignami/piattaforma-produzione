# STATO — Fase 1 Magazzino e pianificazione — 2026-09-04

FASE 1: APERTA

## Fatto finora
- Spec della fase: `docs/superpowers/specs/fase-1.md`, rivista una volta (nessun bloccante).
- Codice sul branch `fase-1`: `ufficio.html` con tre schede, `stampa.html` (solo `tipo=grezzo`),
  `css/ufficio.css`, `css/stampa.css`, `js/ufficio.js`, `js/ufficio/{magazzino,pianificazione,
  impostazioni}.js`, `js/stampa.js`; sette funzioni pure nuove in `js/comune.js`; `js/db.js`
  accetta messaggi d'errore per codice.
- 37 test verdi (31 funzioni pure + 6 coppie di id DOM).
- Backup del passo 6.1 in `Backup app/2026-09-04_1050/` (9 file JSON validati: 10 tipi di
  difetto, 10 rotoli COLLAUDO, sette tabelle vuote).

## Migrazioni applicate
**Nessuna.** La Fase 1 è di solo front-end: tabelle, viste, policy e grant della Fase 0 coprono
già tutto. Verificato in sola lettura sulla produzione prima di iniziare:
`rotoli_grezzi` insert 12 / update 13 colonne, `pianificazione` 6 / 6, `operatori` su tutte le
colonne, **zero** grant su `stato`, `kg_al_metro`, `metri_stimati`, `modificato_*`,
policy `grezzi_upd` = `e_ufficio() and stato = 'grezzo'`, 10 rotoli di collaudo,
10 migrazioni della Fase 0 e nessuna nuova.

Le migrazioni della Fase 0 (dalla `000a` alla `003`) **restano applicate e non vanno rieseguite**.

## Cosa manca per chiudere
1. Pubblicazione: `git merge --no-ff fase-1` su `main` e `git push origin main`.
2. Controllo del sito pubblicato con ricarica forzata.
3. STATO definitivo con `FASE 1: CHIUSA`, `CLAUDE.md` aggiornato, rapporto al committente.

## Se qualcosa si interrompe
Nessuna migrazione è stata applicata: il database è **esattamente** com'era alla chiusura della
Fase 0 (10 rotoli COLLAUDO, 10 tipi di difetto, tutto il resto vuoto). Le prove fatte in locale
hanno inserito e poi rimosso un rotolo `COLLAUDO-0011` e un operatore di prova: verificato che
non sia rimasto nulla. Per riprendere basta ripartire dal passo 8.1 con il branch `fase-1`.
