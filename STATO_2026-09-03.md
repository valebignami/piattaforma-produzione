# STATO — Fase 0 Fondamenta — 2026-09-03

FASE 0: APERTA

## Fatto
- Progetto Supabase `Overland Produzione` creato (ref `nbercxzpjflqfstwrryp`, eu-central-1, piano
  gratuito 0 €/mese). Per liberare il posto (limite di 2 progetti gratuiti) il committente ha
  scelto di mettere in pausa il progetto `Scadenziario` (`cqdmfhdcdvaezmexzxrq`): si riattiva
  dalla dashboard quando serve.
- Backup iniziale (0 tabelle, database appena creato) annotato in
  `Backup app/2026-09-03_1605/README.txt`.
- Migrazioni applicate (vedi tabella).
- `sql/test_regole.sql`: TUTTI I TEST PASSATI (16:2x, prima di 003; ripetuto dopo 003 con il
  test della ripartenza spostata: TUTTI I TEST PASSATI).
- `node --test tests/`: 22 test verdi (21 funzioni pure + 1 id DOM).
- Pagina di login provata in locale (1024×768 e desktop): nessun errore in console, form
  renderizzato, tentativo di login errato → "Email o password non corretti".
- Revisione bug generici (`code-review`, livello high, 8 angoli): 3 correzioni applicate
  (migrazione 003, file CDN con integrity stabile, commenti), 7 annotazioni minori sotto "Aperto".
- Repository GitHub creato: https://github.com/valebignami/piattaforma-produzione (pubblico,
  vuoto); storia ripulita dai `.docx` con `git filter-repo`; remote `origin` collegato.

## Migrazioni applicate (ora locale, 2026-09-03)
| nome | ora | esito |
|---|---|---|
| 000a_ruoli_anagrafiche_grezzi | 16:08 | ok — verifica colonna generata: 8.1 / 802 / grezzo / sistema |
| 000b_lavorazioni_controlli_eventi | 16:12 | ok |
| 000c_viste | 16:16 | ok |
| 000d_rpc | 16:16 | ok |
| 000e_rls_grant | 16:20 | ok |
| 000f_realtime | 16:20 | ok — 3 tabelle nella publication |
| 001_seed_difetti | 16:20 | ok — 10 difetti |
| 002_seed_collaudo | 16:20 | ok — 10 rotoli COLLAUDO-0001…0010, metri_stimati 802 |
| 000e_verifica | 16:22 | ok — tutti gli assert passati |
| 003_ripartenza_ripuntata | 16:55 | ok — correzione da revisione (file `sql/003_2026-09-03_ripartenza_ripuntata.sql`) |

Nessuna sezione di `000_setup.sql` va rieseguita: ogni sezione ha la guardia; le correzioni sono
migrazioni nuove (la 003 è la prima).

## Verificato e come
- Sezione a: insert di prova `PROVA-0001` → `kg_al_metro` 8.1, `metri_stimati` 802, `stato`
  grezzo, `modificato_da` sistema; riga cancellata.
- Sezione e: migrazione `000e_verifica` (RLS su 10 tabelle, nessun grant su colonne riservate,
  viste in sola lettura, niente insert/delete su lavorazioni e rotoli_lavorati fuori dalle RPC).
- `test_regole.sql` come `authenticated` con claims: esempio spec §2.7 (caso C → `/A`, 2.450 kg,
  302 m; secondo giro → `/B`, scarto 70), caso A puro, caso B, tutte le guardie dello spec §5.6,
  più la ripartenza spostata (003). Dopo il rollback il DB contiene solo i 10 rotoli di collaudo.
- Browser locale (`http://localhost:8000/index.html`): screenshot a 1024×768 e desktop.

## Aperto (annotazioni minori dalla revisione, non corrette in questa fase)
- `eventi.tipo` è aggiornabile dal client: un fermo aperto ritipizzato (es. in nota) sfugge alla
  guardia "fermo aperto". Grant previsto dal piano; da valutare in una fase successiva.
- `erroreDiRete` in `js/db.js` classifica per testo del messaggio: da rivedere quando `salva()`
  avrà i primi consumatori (Fase 2).
- Messaggi fissi per i codici 23505/23514 in `js/db.js`: le pagine delle Fasi 1-4 potranno
  passare messaggi specifici.
- `metri` del figlio fornito dal client non validato (nessun check > 0 sulla colonna).
- `grezzi_del` non considera `pianificazione`: la cancellazione di un grezzo pianificato dà un
  errore FK generico (da gestire in Fase 1).
- Guardia "fermo aperto" e aggiornamento del grezzo a fine lavorazione duplicati fra le RPC
  (conformi al piano; eventuale helper in una fase futura).
- L'assert di `000e_verifica` non elenca `rotoli_lavorati.rotolo_grezzo_id` fra le colonne
  riservate (il grant è comunque corretto).

## IN ATTESA DEL COMMITTENTE
- Attivare GitHub Pages sul repository (Settings → Pages → Deploy from a branch → main / root).
- Riattivare `Scadenziario` quando serve (Supabase Pro, o dopo il pilota, quando si decide).
- GitHub Pro → repository privato.

## NON NEL REPO (know-how, solo nella cartella locale)
- `docs/riferimenti/Manuale completo.docx`, `Procedure Produzione.docx`,
  `Progetto_Piattaforma_Produzione.docx`
- `sql/seed_difetti.sql` (applicato come migrazione `001_seed_difetti`)
- `.claude/launch.json`, `RAPPORTO_fase-0.md`, `Backup app/`

## Fatto a mano dal committente
- Creazione dei due utenti Auth `ufficio@overland-ocm.it` e `reparto@overland-ocm.it` (Auto
  Confirm) nella dashboard; mappati in `utenti_app` via connettore (2 righe, confermati).
- Registrazione libera spenta ("Allow new users to sign up" OFF), dichiarato dal committente.
- Creazione del repository GitHub (pagina github.com/new, guidata).
- `git filter-repo` per togliere i `.docx` dalla storia (comando eseguito dal committente nel
  terminale: la riscrittura della storia è bloccata per la skill).
- Pausa del progetto Supabase `Scadenziario` (via connettore, con il sì del committente).

## Scostamenti dichiarati rispetto al piano
- `index.html` carica `dist/umd/supabase.js` (non `.min.js`): il `.min.js` lo genera jsDelivr al
  volo e non è adatto all'integrity SRI.
- Migrazione 003 (correzione da revisione): ripartenza spostata su un altro fermo → durata del
  fermo abbandonato azzerata.
- Un solo commit per le funzioni pure (Task 1-6) invece di sei, e uno solo per le sezioni a-f di
  `000_setup.sql` (Task 7-11) invece di cinque: un commit per voce del PIANO §3.
- Revisione di conformità fatta con `claude-sonnet-5` (Opus sovraccarico, tre tentativi falliti
  con errore 529); resta un modello diverso da quello che ha implementato.
- Le tre funzioni trigger (`imposta_modificato`, `eventi_fermo_durata`, `eventi_ripartenza`) non
  hanno `revoke execute` esplicito: Postgres non permette di chiamare direttamente una funzione
  `returns trigger`, il linter Supabase lo segnala comunque (minore, dal revisore).
- `comune.js` non contiene client e `salva()` (stanno in `db.js`); `package.json` di sola `type`;
  `registra_lavorazione_completa` rifiuta le ripartenze (già nel piano).

MODELLI: implementazione claude-fable-5-1 · revisione claude-sonnet-5 (docs/superpowers/reviews/fase-0-codice-1.md: NESSUN BLOCCANTE)
